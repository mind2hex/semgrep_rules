rules:
  - id: go-tainted-000-Code-Injection
    languages:
      - go
    severity: HIGH
    message: User-controlled input is being used to build or execute program code.
      This can let an attacker inject arbitrary code or alter application logic.
      Validate and sanitize inputs and avoid dynamic code evaluation.
    mode: taint
    pattern-sources:
      - patterns:
        - pattern: |
            func $X(...)
        
    pattern-sinks:
      - patterns:
        - pattern: |
            govaluate.NewEvaluableExpression(...)

      - patterns:
        - pattern-inside: |
            $X := interp.New(...)
            ... 
        - pattern: |
            $X.Eval(...)

      - patterns:
        - pattern-inside: |
            $VM := otto.New()
            ...
        - pattern: |
            $VM.Run(...)
      
      - pattern-either:
        - pattern: |
            expr.Run(...)
        - pattern: |
            expr.Compile(...)
        - pattern: |
            gval.Evaluate(...)
        - pattern: |
            starlark.ExecFile(...)
        - pattern: |
            plugin.Open(...)

      - patterns:
        - pattern-inside: |
            $L := lua.NewState(...)
            ...
        - pattern: |
            $L.DoString(...)

      - patterns:
        - pattern-inside: |
            $RT := wazero.NewRuntime(...)
            ...
        - pattern: |
            $RT.InstantiateModuleFromBinary(...)


    metadata:
      category: security
      subcategory:
        - vuln
      cwe:
        - "CWE-94: Improper Control of Generation of Code ('Code Injection')"
      confidence: MEDIUM
      likelihood: LOW
      impact: HIGH
      owasp:
        - A3:2021 Injection
